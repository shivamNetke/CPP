#include<stdio.h>
int main(void)
{
    int n = 9;

    // if statement with true condition
    if(n < 10)
    {
        printf("%d < 10", n);
    }

    // if statement with false condition
    if(n > 20)
    {
        printf("%d > 20", n);
    }
    return 0;
}