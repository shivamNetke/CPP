#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    int a = 3, b = 4;

    // conditional operator
    int result = (a < b) ? b : a;
    cout<<"the greatest number is : "<<result;

    return 0 ;
}